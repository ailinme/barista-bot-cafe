name: barista_bot_cafe
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Dependencias para testing
  mocktail: ^1.0.3
  integration_test:
    sdk: flutter
  golden_toolkit: ^0.15.0

  # Buenas prácticas de código
  flutter_lints: ^5.0.0
