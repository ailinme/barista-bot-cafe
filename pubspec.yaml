name: barista_bot_cafe
description: App de pedidos para cafetería con robot barista

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI Components
  cupertino_icons: ^1.0.2
  
  # State Management
  provider: ^6.1.1
  
  # Network
  http: ^1.1.0
  dio: ^5.4.0
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # Animations
  lottie: ^3.0.0
  shimmer: ^3.0.0
  
  # Image Handling
  cached_network_image: ^3.3.1
  
  # Date & Time
  intl: ^0.18.1
  
  # QR Code
  qr_flutter: ^4.1.0
  
  # Loading Indicators
  flutter_spinkit: ^5.2.0
  
  # Charts (para dashboard admin)
  fl_chart: ^0.65.0
  
  # URL Launcher
  url_launcher: ^6.2.2
  
  # Permissions
  permission_handler: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Agregar assets
  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/
